services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dsi
      POSTGRES_PASSWORD: dsi_secret_2024
      POSTGRES_DB: dsi_intel
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
